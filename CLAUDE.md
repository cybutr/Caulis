# Caulis — Codebase Guide

## What This Is

In-browser React prototype (Babel transpiled, no build step). Design handoff for a mobile plant-care tracker. **Not production code** — recreate in React Native/Expo or equivalent for a real build.

Entry point: `Caulis Plant Collection.html` loads fonts, React/Babel, then the `.jsx` files in order.

## File Map

| File | Purpose |
|---|---|
| `caulis-core.jsx` | Palette (`C`), font vars, icons, `Specimen`, `statusOf`, shared primitives |
| `caulis-perenual.jsx` | Perenual API service, species library, `speciesCare()` field mapping, `SEED_PLANTS` |
| `caulis-screens.jsx` | Garden, NeedsWater, Scanner, PrintQueue, Settings, bottom nav, context menu, move sheet |
| `caulis-detail.jsx` | PlantDetail overlay, Add/Edit form, photo sheet, identify flow |
| `app.jsx` | All state + action handlers + screen router |
| `ios-frame.jsx` | Simulated iPhone bezel — prototype only, not part of a real app |

## Design Tokens

```js
const C = {
  bg: '#FAFAF7', panel: '#FFFFFF',
  forest: '#2D5016', sage: '#7A9E4E',
  brown: '#6B4C2A', ink: '#2A2A26',
  line: 'rgba(45,80,22,0.08)', hair: '0.5px solid rgba(45,80,22,0.08)',
};
const FONT_SERIF = '"Cormorant Garamond", serif'; // italic, plant names, titles
const FONT_SANS  = '"DM Sans", sans-serif';       // UI labels, body, inputs
```

Status colors: ok `#6E9A3E`, soon `#C98A2B`, needs `#B4472E`.

Radius scale: 11–14 tiles/inputs, 15 card image, 16 buttons, 18 rows, 20–22 cards/sheets, 999 pills.

Spacing: 18–22px side padding, 14px grid gap, 12px list gap, 56px header top.

## Coding Patterns

- All styling is **inline `style={{}}`** — no CSS files, no className styling.
- Never hardcode a color that exists in `C` — always `C.forest`, `C.sage`, etc.
- Icons are inline SVG functions (`Leaf`, `LeafOutline`, `IconGarden`, etc.) — no icon font or external lib.
- `Sprig` SVG (brown, ~20% opacity, absolute bottom-right) appears on every screen as botanical watermark.
- `statusOf(days, every)` returns `'needs' | 'soon' | 'ok'`; `STATUS[status]` gives dot/ring/soft/label.
- `agoLabel(days)` formats the watered-ago string.
- Plant images: `plant.userImage` overrides `plant.image` everywhere.
- QR payload format: `caulis://plant/{id}`. Generated via `qrUrl(data, size)`.
- Customization multiplier/preset pattern: a `caulis_*` localStorage value → an `apply*()` call in `app.jsx` (runs every render, no effect needed) → a module-level `let` in `caulis-core.jsx` read through a plain function (`rad()`, `ds()`, `getStatusStyle()`). Never a parallel styling mechanism or inline `<style>` — always through this same apply/read pair. `STATUS_STYLES`/`STATUS_STYLE_ORDER`/`applyStatusStyle`/`getStatusStyle` (`caulis-core.jsx`) toggle `StatusDot`/`StatusTag` between the classic dot+glow ring and a plain colored text abbreviation (`OK`/`SOON`/`NEEDS`) — wired in Settings → Appearance ("Status indicator").
- **Custom color picker** (`caulis-core.jsx`): `PALETTE_ORDER`/`ACCENT_ORDER` end in a `'custom'` slot alongside the 7 curated presets. `SwatchRow` renders it as a calm outline `IconPipette` tile (`caulis-core.jsx`, same restrained line-art language as `IconDrop`) on a neutral tint — not a fixed rainbow conic-gradient (too vibrant next to the curated swatches) and not the currently-picked hex either (indistinguishable from a preset at default); a small tip-dot in `opt.ring`'s color only appears once the slot is actually selected. Selecting it reveals a `CustomColorPicker` (`caulis-screens.jsx`) — as of v179 this is a fully custom, fully in-theme picker (`HsvSquare` + `HueStrip`, plain HSV math via `rgbToHsv`/`hsvToRgb` in `caulis-core.jsx`, CSS gradients not canvas, `setPointerCapture` drag) plus a plain hex text input, replacing the earlier native `<input type="color">` wrapper — that native swatch popped the browser/OS's own un-restylable hue/saturation grid, which read as un-themed chrome and was called out as unacceptable. `mode` prop (`'accent'` default or `'bg'`) picks which contrast check to show. The independent custom colors (`customPaletteColor` = `caulis_custom_palette_color`, `customAccentColor` = `caulis_custom_accent_color`, `customBgColor` = `caulis_custom_bg_color`/`customBgEnabled` = `caulis_custom_bg_enabled`, all in `app.jsx`) each run through `applyCustomPaletteColor`/`applyCustomAccentColor`/`applyCustomBgColor` (`caulis-core.jsx`) — the first two *before* `applyTheme()`, `applyCustomBgColor` *after* (it overrides `C.bg` post-theme-pick) — every render. `applyCustomPaletteColor`/`applyCustomAccentColor` mutate `PALETTES.custom`/`ACCENTS.custom` in place via `deriveShadesFromHex()`/`deriveAccentDark()`, plain HSL math that derives a full light+dark forest/sage pair (or a single dark accent variant) from one picked hue, matching the curated palettes' "darken for light mode, brighten for dark mode" shape. `contrastWarningFor(hex)` computes real WCAG relative luminance/contrast ratio (not a guess) against both `C_LIGHT.bg` and `C_DARK.bg` for the accent/palette pickers (3:1, WCAG's UI-component/large-text minimum); `bgContrastWarningFor(hex)` does the stricter check for the background picker — body text (`C.ink`/`C.brown`) against the picked background, at the 4.5:1 AA text minimum, since this background sits behind all text, not just accent-colored UI. Neither ever blocks the pick, the color still applies. Settings → Appearance → Background & motion has the "Custom background color" toggle + picker; `index.html`'s pre-mount script also reads `caulis_custom_bg_enabled`/`caulis_custom_bg_color` to set `--bg` before first paint, same reasoning as the font-pairing/palette pre-mount blocks.
- **Icon weight** (`caulis-core.jsx`): `ICON_STROKE_LEVELS`/`ICON_STROKE_ORDER` (`thin`/`regular`/`bold`, `caulis_icon_stroke`) scale every UI icon's stroke width via `applyIconStroke()` → `isw(baseWidth)` — each `Icon*` function (not `Leaf`/`Sprig`, which stay fixed) runs its literal stroke widths through `isw()`. Independent axis from corner-radius density (that scales roundness, this scales line boldness).
- **Garden name** (`caulis_garden_name`, `app.jsx`): free-text override for the "Caulis" wordmark shown at the top of every mobile screen (`ScreenHead`'s `appName` prop, plus `GardenScreen`'s own inline header) — falls back to "Caulis" when empty. Desktop sidebar branding and the About screen keep the literal app name on purpose (app identity vs. garden nickname).
- **Font pairing** (`caulis_font_pairing`, `caulis-core.jsx`): `FONT_SERIF`/`FONT_SANS` are no longer literal font stacks — they're `'var(--font-serif)'`/`'var(--font-sans)'`, so the ~540 existing `fontFamily:FONT_SERIF/FONT_SANS` call sites across every file never had to change. `FONT_PAIRINGS`/`FONT_PAIRING_ORDER` (`classic`/`literary`/`editorial`) each name a serif+sans pair; `applyFontPairing(key)` writes the two CSS custom properties onto `document.documentElement` and, for any pairing other than `classic` (already eagerly loaded via `index.html`'s `<link>`), lazily injects that pairing's Google Fonts `<link>` — fetched only the instant a user actually picks it, never upfront. `index.html` seeds `--font-serif`/`--font-sans` on `:root` for pre-mount paint, and its dark-mode pre-mount `<script>` also reads `caulis_font_pairing`/`caulis_palette` from localStorage to set the same CSS vars (plus a palette-accurate splash icon color and `theme-color` meta) before first paint — necessarily duplicating `FONT_PAIRINGS`' href/family table and a light/dark hex table for the 7 curated palettes, since there's no build step to share modules with the static shell; `applyFontPairing()`/`applyTheme()` re-run the same work harmlessly once React mounts. Settings → Appearance renders all three pairings as live font-preview rows (each option's own name set in its own `serif`/`sans`), not a plain `OptionList`, so a pick is WYSIWYG.

## Cozy depth system (grain, warmth, hero banner, micro-interactions)

- **Real grain texture** (`index.html` + `caulis-core.jsx`): a single fixed full-viewport `.grain-overlay` div (`mix-blend-mode: overlay`, `isolation: isolate`) driven by an SVG `#grainFilter` (`feTurbulence type="fractalNoise"` → `feColorMatrix` zeroing RGB, alpha-only so it's theme/palette-neutral). `GRAIN_LEVELS`/`GRAIN_ORDER` (`subtle`/`medium`/`bold`/`deep`, `caulis_grain_intensity`) follow the exact same apply/read pattern as icon stroke/radius density: `applyGrainIntensity(level, textureOn)` called every render in `app.jsx`, a module-level `let grainLevel`, read via `grain()`. The SVG filter's `baseFrequency` does NOT reliably take `var()` across engines, so it's mutated imperatively on `#grainTurb`; only the blend layer's CSS opacity goes through `--grain-opacity`. Only active when Settings → Appearance → Background texture is `paper` (replaces the old two-dot-layer CSS fake); `dot`/`none` unchanged. Grain intensity sub-control reveals conditionally, same pattern as the custom color picker's swatch reveal. `deep` (v179) is one bolder top-end preset added on user request for "more texture/depth" — still alpha-only fractal noise, never a literal burnt/stained-paper PNG, so it stays in the restrained-premium lane rather than the "cheap Instagram filter" look earlier research flagged. Each `GRAIN_LEVELS` entry also carries a `vignette` value driving a second fixed full-viewport `.vignette-overlay` (`index.html`, `--vignette-opacity`) — a radial `rgba(107,76,42,…)` wash visible only at the extreme viewport edges (`transparent 60%` in the center), tied to the same texture toggle/intensity rather than a separate setting. Reads as "cozier," never covers content.
- **Warmth helpers** (`caulis-core.jsx`): `warmEdgeStyle(strength)` (a low-opacity `C.brown`-tinted top-edge gradient, ceiling matched to `C.line`'s own 0.08 opacity) and `PHOTO_FRAME_SHADOW` (a subtle inset shadow) are the one shared "aged photo" vocabulary — reused on the Garden hero banner and Plant Detail's hero frame, never invented per-screen. Texture stays monochrome/grayscale-alpha; warmth always comes from palette + typography.
- **Garden hero banner** (`GardenHeroBanner`, `caulis-screens.jsx`): a rotating photo collage above/below `GardenScreen`'s header. `pickGardenHero(plants)` scores candidates with a real photo — `+3` needs-water, `+2` recently-added (plant id proxy), `+1` not one of the last 5 featured (`caulis_hero_last`, rotating array) — tie-broken randomly, so "needs water" is a signal, never an override (never nags every day). Returns the winner (`plant`/`why`, used for the caption and `caulis_hero_last` bookkeeping) plus a deduped top-`HERO_TILES` (3) `group` for the collage. **Live rotation (v179)**: a `useEffect` re-picks whenever the day rolls over or the most-urgent (needs-water) plant id changes — tracked in a `lastPick` ref — without ever remounting the component; a 5-minute interval tick keeps that check alive across a long-open session even if `plants` itself doesn't change. Previously this only ever computed once per mount (the `!hero` guard), so the only way to see a "new" pick was a hard remount — what read as the whole page "hot reloading." A rotation now **crossfades**: the outgoing pick (`prevHero`) stays mounted, absolutely positioned, fading `opacity 1→0` over 420ms while the incoming one fades in via the `fade` keyframe, both via a shared `renderContent(hero, fadingOut)` closure so old/new render identically. Computed initial pick via a `useEffect` once `plants` actually has data (not a lazy `useState` initializer, which would freeze on an empty list if plants were still mid-sync on first mount). **Collage, not one full-bleed photo**: `compressImage` (`caulis-detail.jsx`) caps stored photos at 1024px, and stretching one such photo across the banner's full width via `objectFit:cover` forced a heavy upscale (worse on wide desktop layouts, worse again under Ken Burns) — visibly pixelated. Instead the banner tiles up to 3 photos: the scoring winner large on the left (`flex:'0 0 62%'`), up to two supporting candidates stacked smaller on the right — each tile covers far less display area so needs far less upscale from the same source resolution. Falls back to one full-bleed image only when a garden has just one plant with a photo. Warm-edge/frame/scrim overlays sit above the whole collage as one shared layer (not per-tile) so it reads as a single considered composition. Crossfades in (`heroIn`); the featured tile gets the one-way Ken Burns zoom (`kenBurns`, `scale(1)→1.06`, 16s), supporting tiles get a gentler `kenBurnsTile` (`scale(1)→1.03`, staggered `animationDelay`) since their tighter crop shows zoom artifacts sooner — both `reduceMotion`-gated. Ken Burns is deliberately NOT applied to the actively-swiped `PhotoCarousel`, only this hero and Plant Detail's hero frame. A broken/corrupt tile photo hides itself via `onError` rather than showing a broken-image icon. **Desktop sizing (v179)**: raised from a flat `maxWidth:640` (read as small/stranded next to a wide sidebar layout's available width) to `width:'100%', maxWidth:980` and `height:210` (was 150) — scales with the actual column instead of a fixed number well under it. Caption/name sit asymmetrically (left-anchored, `right:'26%'`, not centered) with a small rotated `LeafOutline` accent in the top-right corner, and always reference the single scoring winner even though multiple photos are shown, so the banner still reads as personal/specific rather than generic.
- **Hero banner is fully user-controlled**: Settings → Appearance → "Garden hero photo" (`caulis_hero_banner`: `off`/`below`/`above`) — off disables it outright regardless of motion settings, `below`/`above` place it relative to the greeting/title block.
- **Secret "Layout Lab"** (`GardenHeroBanner`): tapping the hero banner 7× (same discovery shape as the Developer panel's 7-tap version row, no PIN — this only repositions a decorative element) sets `caulis_layout_lab_unlocked` and shows a one-time toast. **v179 fix**: the previous "suppress the 2nd+ tap" approach still opened Plant Detail synchronously on tap 1 — Plant Detail is a full-screen overlay that covers the banner, so the user had to manually close it before tap 2 could ever land, well outside the 1.4s counting window, making 7 taps unreachable in practice (confirmed by a realistic tap-and-wait Playwright test; the earlier "passing" test had used synthetic same-frame event bursts that never let a real overlay render in between). Reworked to the classic single-tap-vs-burst pattern instead: a tap never opens synchronously. It arms a short deferred open (`openTimer`, 280ms — under the ~300–500ms double-tap threshold used everywhere); a second tap before it fires cancels the pending open and keeps counting, so a genuine rapid-tap burst never opens Plant Detail mid-sequence, while a truly isolated single tap still opens, just imperceptibly later. Once unlocked, a plain tap (checked in `onUp`) opens immediately again — the deferred-open dance is only needed pre-unlock. Long-pressing (480ms) the hero enters a real drag mode (dashed sage glow + "Layout Lab" pill + Done chip): dragging it up past the header moves it to `above`, dragging down returns it to `below`, persisted through the same `onSetHeroBanner`/`caulis_hero_banner` as the plain Settings toggle. This is additive — the Settings toggle stays the simple, discoverable path; Layout Lab is an extra secret layer for whoever finds it.
- **Watering micro-celebration** (`NeedsRow`, `caulis-screens.jsx`): swiping a row to water triggers a small pulse (`waterPulse`, spring easing) + soft radial glow (`waterGlow`) behind the `StatusTag`, plus 1-2 particles drifting UP and fading (`driftUp`) — reads as "growth," deliberately smaller than and distinct from the falling-confetti treatment reserved for water-all/badges/milestones. Gated by `reduceMotion` in JS (not just the global CSS override) since it's triggered imperatively, not mounted.
- **Staggered card entrance**: `cardEntranceStyle(idx)`/`CARD_STAGGER_CAP` (`caulis-core.jsx`, cap 10) fade+rise `PlantCard`s on Garden grid mount (`cardIn`, 28ms stagger); beyond the cap, or under `reduceMotion`, cards mount at final position with no animation.
- **Pull-to-refresh reskin** (`app.jsx`): the plain scaling dot is now a `LeafOutline` that rotates proportional to pull distance and switches its ring color `C.line` → `C.sage` at the trigger threshold, matching the `isw()` icon-stroke system.
- **Empty-state warmth**: `EmptyGarden`, Needs-Water's "all caught up", and Print Queue's empty state all get a slow `sproutBob` loop on their icon plus warmer copy (still gated by `reduceMotion`).
- **Toggle feedback** (`ToggleKnob`, `caulis-screens.jsx`): every Settings on/off switch is now this one shared component — a brief confirming ring-flash (`toggleFlash`) on change via `key={on}` remount, instead of an instant flat flip. Converted every existing hand-rolled toggle switch to it (search `ToggleKnob` — don't reintroduce a bespoke inline switch elsewhere).
- **Reduce motion**: every animation above is a pure CSS `@keyframes` + `animation` property, so it's automatically neutralized by the existing global `html[data-rm="1"] *` override (`index.html`) in addition to any JS-level `reduceMotion` gating already present — belt and suspenders, never rely on only one.
- **Custom reminders are edit-gated** (`caulis-detail.jsx`): `PlantDetail`'s view-mode Reminders section only renders once a plant has 1+ schedules (mark-done and tap-to-edit-existing stay available there) — no empty state, no "add" prompt cluttering the normal view. Adding a NEW reminder only happens from the Add/Edit plant form (`AddPlant`'s own "Reminders" `Field`, gated on `editing` — a brand-new unsaved plant has no id to attach a schedule to). `AddPlant` looks up the live plant from the `plants` prop each render (not the `editing` snapshot captured when the form opened) so a just-added reminder appears immediately.
- **Surfaces reached in the broadened pass** (previously flat/untouched relative to Garden/Plant Detail): the species-suggestion loading state in the Add/Edit form (`caulis-detail.jsx`) now shows two `shimmerStyle()` skeleton rows instead of a bare spinner+text line — `shimmerStyle()` (`caulis-core.jsx`) existed unwired until this pass. `ContextMenu` (long-press menu, `caulis-screens.jsx`) gained a `warmEdgeStyle` top wash on its sheet plus a `slideFromL` stagger per row. `MoveSheet`'s room list picked up the same stagger. The Scanner `Viewfinder` got a soft warm radial glow behind the frame (no animation added — the existing 2.4s scanline stays the only motion there). `DesktopSidebar` nav rows (`SidebarItem`, new component) gained a hover lift/tint and a warm left-accent bar on the active tab — desktop had received none of today's depth pass until now.

## Time with this plant (anniversary + seasonal touches, v181)

- **`addedAt`** (`app.jsx`): a real creation timestamp, set on every new plant in `savePlant`'s no-`data.id` branch. Existing plants (saved before this field existed) get it backfilled once in the `caulis_plants` `useState` initializer — `p.addedAt || Date.now()`, i.e. "now," never a fabricated past date, so an old plant's "with you for…" clock honestly starts ticking from the day this shipped. Rides through `mergePlants` (`caulis-firebase.jsx`) with zero special-case code, same as any other plant field (that merge is a generic whole-object 3-way diff, not a field-by-field allowlist).
- **`plantMilestoneLabel(addedAt)`** (`caulis-core.jsx`): 1 month / 6 months / 1 year, then yearly ("2 years with you", …). Only returns non-null for a short window after crossing (`MILESTONE_WINDOW_DAYS` = 12) — a passing callout, not a permanent stat readout. Rendered in `PlantDetail` just under the location/status row, small italic sage text with a leaf glyph.
- **Badge**: `plant-anniversary` / "One Year Together" (`caulis-badges.jsx`) — per-plant `addedAt` crossing 365 days, distinct from the pre-existing `garden-anniversary` ("One Year In", garden-wide, earliest watering stamp) and `secret-old-friend` (30 waterings for one plant, not time-based). Reuses `BadgeIconCalendar` deliberately — that icon is already excluded from the ambient wallpaper layer (`AMBIENT_EXCLUDE_ICONS`) since a clock-face glyph drifting near a real on-screen timestamp reads as a false affordance; this badge inherits that exclusion for free.
- **Seasonal hero captions**: the Garden hero banner's quietest fallback caption (`why` is neither `needs` nor `recent` — nothing urgent, nothing new) now varies by real calendar month (`currentSeason()`/`SEASONAL_QUIET_CAPTIONS`, `caulis-screens.jsx`), picked deterministically by day-of-month so it doesn't flicker between options within one visit. **No seasonal color tint was added** — a real tint risked competing with the user's own chosen palette/accent, which this app treats as sacred everywhere else (custom color picker, custom bg color, font pairing all layer strictly on top of, never over, the user's picks). Wording-only was the honest call here.
- **Hero "why this photo"**: a tiny `i` chip next to the hero caption (`GardenHeroBanner`) reveals one honest sentence about why that photo was picked (needs-water nudge / recently-added / quiet-rotation), via the same toast vocabulary as the Layout Lab's `labToast`. `stopPropagation` keeps it inert to the tap-to-open/7-tap-unlock/drag-to-reposition gestures already layered on this same element.

## Plant Story (`caulis-detail.jsx`)

- A new chronological view, distinct from the tile-based `PlantDetail` on purpose: the detail view answers "what does this plant need right now," Plant Story answers "what has actually happened here." Opened via "View this plant's full story" under the Watering log tile.
- `buildPlantStory(plant)` merges three real sources into one sorted (newest-first) list: the `addedAt` moment, every watering `history[]` stamp, and every custom reminder schedule's own `history[]` (each schedule's own icon/label, via `SCHEDULE_ICONS`). Grouped by calendar year with a thin connecting line, same "log" shape as a lot of activity-history UIs but drawn in this app's own vocabulary (`C.hair`, `FONT_SERIF` italic labels, `cardEntranceStyle` stagger).
- **Photos are shown, not dated**: `photos[]`/`userImage` have no per-photo timestamp anywhere in this data model (`compressImage` never stored one) — rather than fabricate an ordering, the plant's current photos render as a plain undated gallery strip above the timeline. Honest scope-limit, not a bug.

## Print tag warmth (v181)

- The actual physical artifact — the label someone tapes to a pot — was previously a bare QR code + name + latin, with zero brand identity once it left the app. `printAll` (`app.jsx`) now adds one small leaf glyph + italic "Caulis" wordmark under the latin name on every printed label (`.brand` CSS class in the generated label-sheet HTML), respecting the existing monochrome-print toggle (`brandCol` follows `mono` exactly like `qrInk`/`nameCol`). The in-app QR preview in `PlantDetail` mirrors the same small mark so the preview matches what actually prints.

## Badges view (`caulis-badges.jsx`)

- `BadgesView` was missing the `Sprig` watermark every other full-screen overlay carries (the same class of gap the Doctor overlay had before its same-day fix) and had no entrance animation on its earned/locked rows — both fixed: `Sprig opacity={0.12}` plus `cardEntranceStyle(i)` per row (locked rows continue the index count from where earned rows left off).

## Weekly Digest

- **`WeeklyDigest`** (`caulis-screens.jsx`), opened via Settings → Notifications digest preview / `onOpenDigest`: redesigned in v180 from a single-photo banner + two flat stat tiles into a genuinely richer "weekly recap" using the same tokens (`C`, `rad()`, `ds()`, `FONT_SERIF`/`FONT_SANS`, `warmEdgeStyle`/`PHOTO_FRAME_SHADOW`) as everything else shipped this pass.
- **Hero banner**: `DigestHeroCollage` reuses `GardenHeroBanner`'s exact multi-tile collage technique (up to 3 recently-watered photos, scoring winner large-left) instead of one stretched full-bleed photo — the same upscale/pixelation problem the Garden hero banner had, same fix, deliberately the same component family rather than a second parallel implementation so future collage tweaks only happen once. Asymmetric: a small "N waterings" chip breaks the top-right corner instead of a centered banner strip, caption anchored bottom-left with room cut out on the right for the chip.
- **New data surfaced**: `WeekActivityBars` — a plain 7-bar div chart (not a charting library, same restrained spirit as the admin panel's own gauges) showing watering counts per day of the last week, today's bar bolded. "Watered this week" lists each individual plant watered with a relative `agoLabel()` (not just a count). "Other reminders completed" aggregates every plant's `schedules[].history[]` within the week (same midnight-anchored stamp shape as watering history) — plant + schedule label + when, mirroring the push-digest copy's own reminder rollup so the in-app view is at least as rich as the notification body.
- **Layout**: an asymmetric stat row (one wide "headline" waterings tile with the activity chart inside, one narrower stack of smaller tiles) replaces the old rigid 2-up equal grid. Desktop caps content at `maxWidth:720` centered, rather than stretching edge-to-edge.

## Location tags (color + icon)

- **Scope choice**: tags attach to LOCATIONS, not individual plants — rooms are a small, stable, named set that repeats across many plant cards, so one tag pays off everywhere that room's name appears; per-plant tagging would be much higher cardinality for the same visual payoff.
- **Storage** (`app.jsx`): `locationTags` — `{ [roomName]: {color, icon} }` — persisted to `localStorage('caulis_location_tags')` via `setLocationTag(name, tag)`. `color` is a `PALETTE_ORDER` key (`forest`/`teal`/…/`custom`) or `'custom'` + a stored `hex`; `icon` is a `SCHEDULE_ICONS` key — both reuse the exact sets built for the global theme accent picker and custom reminder schedules, no parallel color/icon vocabulary invented. `renameLocation`/`removeLocation` (`app.jsx`) carry/drop the tag entry alongside the room name so it never survives under a stale key.
- **Deliberately local-only, not synced**: `locations[]` is part of the synced garden payload with a strict server-side DB column and a 3-way `mergeArray` (see `caulis-firebase.jsx`) — that merge path has caused real incidents when widened carelessly. `locationTags` is a per-device visual preference instead, kept out of `pushGarden`/`mergeGarden` entirely, so this feature adds zero risk to the sync path. A plain-string location with no tag renders exactly as before (dot/pin fallback) — fully additive, no migration needed for existing data.
- **Helpers** (`caulis-core.jsx`): `locationTagColor(tag)` / `locationTagIcon(tag)` resolve a tag to a hex string / icon component, `null` when untagged (every call site falls back to the pre-existing look).
- **Editor** (`LocationTagEditor`, `caulis-screens.jsx`): inline under each room row in Settings → Garden → Rooms (`LocationsManager`), toggled by tapping a small color-swatch/pipette button next to the room name. Reuses `SwatchRow` + `CustomColorPicker` for color and the `SCHEDULE_ICON_ORDER` 12-icon grid for icon, plus a "Clear tag" action.
- **Rendered wherever a location shows as a plain chip/label**: Garden screen's room filter chips and grouped-by-room `RoomHeader`, each `PlantCard`'s location line (small color dot), `LocationPill` (Print Queue rows), and the room-picker list in `MoveSheet` (also picked up a staggered fade-in as part of the broader depth pass).

## Garden grid depth pass (v187): attention-weighted grid + Journal view

- **Attention-weighted grid** (`caulis_grid_weighted`, Settings → Garden → "Emphasize plants that need attention"): opt-in, default off, preserves the exact original uniform grid when off. `GardenScreen`'s `cardSpanFor(p)` gives any plant that's `statusOf(...) === 'needs'` or was `addedAt`'d within the last 3 days a `gridColumn: 'span 2'` — plain CSS Grid spanning, no masonry library. The grid container adds `gridAutoFlow: 'dense'` (mobile flat grid, desktop `auto-fill` grid, and the location-grouped grid all three) only while the setting is on, so spanned cards auto-pack instead of leaving holes. `PlantCard` takes a `span` prop threaded straight from `cardSpanFor` — bumps `Specimen` image height and name font size a notch when spanned so the extra width reads as "featured," not just "stretched." **Reorder interaction**: dragging shares the grid with spanning, and drop-target math (`useReorder`'s nearest-center hit test) gets unreliable once span sizes vary, so `dragEnabled` is `false` whenever `gridWeighted` is on (in addition to its existing search/filter/select-mode gates) — a small caption ("Manual reorder is paused while emphasis is on.") explains why, rather than silently breaking drag.
- **Garden Journal view** (`caulis_garden_view`, `'grid'`/`'journal'`, persisted via the same `GS.get`/`GS.set` pattern `caulis_g_sort` already uses): a second, alternative lens on the same filtered plant set (`matched`), not a parallel data model. Toggled by a small grid/list icon pair added to `GardenFilterBar` (left of the existing sort chips); the sort chips and location-grouping only make sense for the grid, so they hide while journal is active, but the search bar and status/location filter chips still apply to `matched` in both modes. `buildGardenJournal(plants)` (`caulis-screens.jsx`) calls `buildPlantStory(p)` (already built per-plant in `caulis-detail.jsx` for Plant Story) for every plant and tags each event with its `plant` — garden-wide aggregation of the exact same event assembly, not a second history system. Events group under relative day headers (`journalGroupLabel`: Today/Yesterday/weekday/"Month day") rather than by year like the per-plant view, since a garden-wide feed skews recent. `GardenJournalView` also surfaces a horizontal "Needs water now" quick-action strip (tap opens the plant via `onOpen`, a small Water pill calls `onWaterOne` directly — same callbacks the grid already uses, no parallel action path) and an active-`plantMilestoneLabel` list above the chronological log. Per CLAUDE.md's existing Plant Story note, photos still have no per-photo timestamp in this data model, so "new photo added" is deliberately not fabricated into the feed — an honest scope limit, not an oversight.

## Settings polish + per-plant featuring pass (v188)

- **`SwatchRow`/`TexturePicker` overflow-clip fix** (`caulis-screens.jsx`): both shared row components used `paddingBottom:2` with zero horizontal padding on an `overflowX:'auto'` flex container — the selected-swatch ring (`boxShadow: 0 0 0 2px bg, 0 0 0 4px ring`, extends ~4-6px past the swatch's own box) got clipped by the scroll container's own edge whenever the first/last item was selected. Both now use `padding:'4px 5px 6px'`, enough for the largest ring offset used anywhere either component is called (accent color, nav-tab-highlight accent, background color, texture picker — all four share this fix since they share the component).
- **Dark mode vs. custom background color — real conflict, not just unclear docs**: `applyCustomBgColor(hex)` (`caulis-core.jsx`) overrode `C.bg`/`C.panel`/`C.input` but left `C.ink`/`C.brown` exactly as Dark mode's `applyTheme()` had set them. Concretely: Dark mode on (light-mode-facing `C_DARK.ink` = pale `#DCE8CC`, meant for a dark background) + a custom background color that happens to be light (e.g. the picker's own default `#FAFAF7`) = pale-on-pale, genuinely near-illegible text — reproduced live via Playwright, not hypothetical. Fixed at the source: `deriveBgSurfaces(hex)` now also returns a `dark` flag (the same lightness check it already used for panel/input), and `applyCustomBgColor` uses it to set `C.ink`/`C.brown` from whichever of `C_LIGHT`/`C_DARK` actually matches the picked background's real lightness — independent of the Dark mode toggle's own state. This makes the relationship honest: **Dark mode still controls the accent palette's light/dark shade** (forest/sage variant, status colors, tints) whenever no custom background is active or the custom background is later cleared; **text color (ink/brown) always follows whatever background is actually on screen** — the custom pick's own lightness once one is active, Dark mode's ink otherwise — so the two settings can never fight each other into illegibility again. Settings → Appearance now spells this out: the Dark mode row's subtext appends the explanation whenever a background color choice is active, and the Background color row's subtext notes it overrides Dark mode's background while Dark mode still governs palette shade.
- **Per-plant "Feature this plant"** (`plant.featured`, boolean, `app.jsx`): a manual, deliberate per-plant override of card size in the Garden grid — distinct from v187's automatic attention-weighted grid (status/recency-driven). Rides the plant object through the existing sync/merge path with zero special-case code, exactly like `addedAt`/`schedules`. `toggleFeatured(id)` (`app.jsx`, next to `movePlant`) flips it; surfaced as a "Feature this plant (bigger in Garden)" / "Unfeature (back to normal size)" row in `ContextMenu` (long-press menu) — chosen over a dedicated app-wide "layout edit mode" since this is a per-plant, occasional choice, not a whole-mode activity, and the long-press menu is already the established place for per-plant actions (move/edit/remove). `GardenScreen`'s `cardSpanFor(p)` checks `p.featured` FIRST, before the `gridWeighted` automatic check — a manual feature pick always renders large regardless of whether "Emphasize plants that need attention" is on, since it's an explicit choice, not a heuristic. `hasFeatured` (any plant featured) now also triggers `gridAutoFlow:'dense'` and pauses drag-reorder, the same as `gridWeighted` already did, so a spanned card always packs cleanly and doesn't fight manual reordering; the existing "Manual reorder is paused…" caption's copy was broadened to cover both cases. Verified live: featuring a plant visibly doubles its grid span; unfeaturing reverts it; both work with the automatic weighted-grid toggle on or off.
- **Second Layout Lab trigger — the Garden greeting text** (`GardenScreen`, `caulis-screens.jsx`): the day/time-of-day greeting (`todayGreeting()`, e.g. "Wednesday evening") now also unlocks Layout Lab via 7 rapid taps, calling the same `onUnlockLayoutLab`/toast path the hero banner's 7-tap already uses. Unlike the hero banner, this text has no competing single-tap "open something" action, so it uses a plain rapid-tap counter (`greetTaps`/`greetTapTimer`, 1400ms window) with no deferred-open disambiguation needed — simpler by design, not a missed edge case. Verified live with realistic sequential taps (~180ms apart, not a synthetic burst): unlocks correctly, shows its own toast ("Layout Lab unlocked — long-press the hero photo to drag.").
- **Hero banner 7-tap unlock — re-verified, confirmed NOT broken**: re-tested live with realistic sequential taps (~220ms apart) rather than a synthetic same-frame burst. Plant Detail never opened mid-sequence and the 7th tap unlocked Layout Lab correctly with its toast. The mechanism itself is sound; a user's difficulty landing it by hand is a real-world timing/target-size UX friction (a small, sometimes-animating collage target), not a code defect — which is exactly why the easier greeting-text trigger above was added as a second, more forgiving path, rather than re-touching the hero banner's already-fixed tap-vs-open logic again.
- **Plant Detail editorial layout rework — investigated, deliberately skipped again**: browser tooling worked fine this session (no lock), so the option was genuinely open. Didn't ship it: the existing `InfoTile` grid already reads as clean/considered, current session time was fully spent on the five items above (all explicitly requested, all verified live), and a layout rework risks the "different, not necessarily better" outcome the prior agent was already wary of. Left as an open idea, not reattempted without a clearer design direction.

## Dark mode merged into the background-color picker (v189)

- **Dark mode is no longer its own toggle.** The standalone "Dark mode" row (Settings → Appearance) is gone entirely — the v188 note above describing it as a still-separate control that "still governs your accent palette's shade" is superseded by this section. Picking **Black** from the existing Background color `SwatchRow` (Settings → Appearance → Background & motion — off/White/Black/palette-tint/Custom) is now the *only* way to put the app in dark mode; picking White, a palette tint, "Light" (the renamed former `'off'`/theme-default slot), or a Custom color that isn't itself dark returns it to light. There's no independent `caulis_dark` flag being written anymore.
- **`darkFromBgChoice(choice, customHex)`** (`caulis-core.jsx`) is the single source of truth: it resolves the picker's choice through the existing `resolveBgColorChoice()` into a hex-or-null, then reuses `deriveBgSurfaces(hex).dark` — the *same* lightness check that already derived panel/input/ink for a custom background since v188 — to decide dark-or-light. `app.jsx` no longer holds a `darkMode` `useState`; it's now a plain `const darkMode = darkFromBgChoice(bgColorChoice, customBgColor)` recomputed every render from `bgColorChoice`/`customBgColor`, then fed into `applyTheme(darkMode, palette, accent)` exactly like before. Palette tints (`BG_TINTS`, derived at `l:96`) are always light by construction — there's no dark palette-tint variant, a deliberate limitation rather than an oversight.
- **Migration for existing users** (`app.jsx`'s `bgColorChoice` `useState` initializer): anyone with the old `caulis_dark` flag explicitly set gets folded straight into the background-color choice on first load after this update — `'1'` → `bgColorChoice = 'black'`, `'0'` → `'off'` (stays visually identical to before: `'off'` still resolves through `resolveBgColorChoice` to `null`, i.e. plain `C_LIGHT`, unchanged). A user who had picked a light custom background *while* the old Dark mode toggle was separately on will see that combination resolve to light now (their custom pick's own lightness wins) — this is the intended, honest outcome of merging two controls that could previously fight each other (see v188's note above); it's a one-time, one-directional behavior change, not a bug.
- **Fresh installs with no stored preference at all** now seed `bgColorChoice` from `matchMedia('(prefers-color-scheme: dark)')` — `'black'` if the OS is dark, `'white'` if not — so system dark-mode detection still works as a sensible *default*, it just no longer keeps overriding the choice on every load once the user has picked something explicit (matching how every other `caulis_*` preference in this app already behaves: read once at init, then user-controlled).
- **`index.html`'s pre-mount script** mirrors this exact resolution order before first paint (`bgChoice` read → `'black'`/`'white'`/`'off'`/`'custom'` branch, `isHexDark()` — a small inline duplicate of `deriveBgSurfaces`'s `(max+min)/2 < 50` lightness check, since `caulis-core.jsx` hasn't loaded yet) so there's still no flash-of-wrong-theme on load, same guarantee the old `caulis_dark` read used to provide. Falls back through the same legacy-flag-then-OS-preference chain as `app.jsx`'s own initializer.
- **`_crashTheme()`** (`app.jsx`, feeds the `ErrorBoundary` wrapper that can't trust React state) was updated the same way — it now derives from `caulis_bg_color_choice`/`caulis_custom_bg_color` first, falling back to the legacy `caulis_dark` flag only when neither has ever been set.
- **Settings copy**: the Background color row's subtext now says outright that this is what controls dark mode; the picker's "Theme default" slot was relabeled **"Light"** (still the same `'off'` storage key/behavior — it always resolves to `null`, i.e. no override, which is unambiguously light now that there's no separate toggle it could otherwise be paired with).

## Layout Lab desktop-unreachable bug (v189) — real cause, not a viewport issue

- The v188 note above ("Hero banner 7-tap unlock — re-verified, confirmed NOT broken") tested with a fresh account that had **zero earned badges**, so `AmbientBadgeLayer` (`caulis-badges.jsx`) never mounted during that verification (`wantVisible = enabled && held.length > 0`) — the actual bug only reproduces once a real garden has earned badges *and* Settings → Appearance → "Ambient badges" is on, which is exactly a real user's normal state, unlike a disposable test account.
- **Root cause**: `AmbientBadgeLayer`'s occlusion probe (`_looksClickable`, `caulis-badges.jsx`) decides whether a floating ambient badge is allowed to become clickable at its rolled position by checking whether the real element already sitting at that screen point has `cursor:'pointer'` anywhere up its ancestor chain — if not, it assumes "nothing real is there" and makes the badge itself interactive on top of it. The Garden greeting text (`todayGreeting()`, `GardenScreen`, `caulis-screens.jsx`) — this milestone's *second* Layout Lab trigger, added specifically because the hero banner's 7-tap target was hard to land — had `cursor:'default'` on its `onClick={tapGreeting}` div. That made it invisible to the occlusion probe, so whenever a badge happened to roll into that exact position, the badge would silently intercept every subsequent tap meant for the 7-tap counter, and `tapGreeting` would never fire.
- This is **not inherently viewport-specific** — the same CSS-based check runs identically on mobile and desktop — but it fully explains a desktop-only failure report: the user's real garden already has earned badges and ambient badges on, so the occlusion bug reproduces reliably there, while whichever session verified this on v188 simply hadn't earned any badges yet to expose it.
- **Fix**: one line — `cursor:'default'` → `cursor:'pointer'` on that div (`caulis-screens.jsx`). This is a fix to the application code the occlusion probe *reads*, not a change to the occlusion algorithm itself (that system — `AmbientBadgeLayer`, `_looksClickable`, the probe/restore dance — was left untouched, per this codebase's standing rule against touching the badge occlusion system). The hero banner's own container already had `cursor:'pointer'` and was never affected.
- Both Layout Lab triggers (hero banner long-hold-then-7-tap, greeting text plain 7-tap) were re-verified live in a desktop viewport (1280×900) with realistic sequential mouse clicks (~200ms apart, not a synthetic burst) with several badges earned and ambient badges on — both now unlock Layout Lab correctly on desktop.

## Plant Detail editorial rework — shipped (v190)

- Third attempt at this task, this time built and shipped. Two prior sessions had declined to touch `PlantDetail`'s boxed 2-up `InfoTile` grid (light/watering) citing screenshot tooling issues or "not confident it's better." Built it, screenshotted before/after at 390px and 1280×900 via a disposable local test garden (Monstera, light/watering/care/fact/toxicToPets all filled in), and made the call: **ship**.
- **What changed**: `CareNarrative` (new, `caulis-detail.jsx`) replaces the light+watering `InfoTile` pair with one flowing `FONT_SERIF` italic sentence — "☀ Prefers bright indirect light. 💧 Average watering — about every 7 days — keeps it happy." — small inline icon accents (`InlineGlyph` wrapper, `IconSun`/`IconDrop` at `s=15`, matching `isw()`) sitting at the baseline via `verticalAlign:-3`, not full tile icon chips. `plant.care` (a real descriptive sentence from the API/library, already prose) now renders as a plain `FONT_SANS` paragraph directly below, and `plant.fact` as a small italic sage aside with a tiny leading `LeafOutline` glyph — no more three separate bordered boxes for what was already three sentences of running text.
- **Hybrid, not all-or-nothing**: `toxicToPets` and the room-light-mismatch warning stayed OUT of the prose — both are safety/alert information a user needs to notice at a glance, not read as narrative, so they became compact colored pills (same visual language as the existing "Propagated from X" pills higher up this screen) instead of full InfoTiles. Reminders (`ScheduleRow` list) and the Watering log (stat counts + date chips) stayed exactly as bordered `InfoTile` cards — both are genuinely stat-dense/actionable (mark-done taps, total/30-day counts, date history), which is precisely the "reserve chrome for actionable items" carve-out this task specified. The water button and Ask-the-doctor CTA were untouched.
- **Verdict backed by the screenshots**: the flowing paragraph reads warmer and more considered without sacrificing scan speed — it's still only 2 short lines, the icons still mark "light" vs "watering" as clearly as the old tile headers did, and removing four bordered boxes down to one flowing block plus two small pills measurably reduced the vertical chrome before reaching the watering-log data. The one real defect found in testing — an inline "i" glyph before the fun-fact sentence rendering as a stray "¡"-looking character at 14px italic — was caught in the first screenshot and fixed (swapped for the same small `LeafOutline` glyph used elsewhere) before shipping, not left in.
- Nothing about data shown, editing, or interaction changed — this was presentation-only, verified against the same plant object shape.

## State Shape (`app.jsx`)

```js
plants[]      // { id, name, latin, location, days, every, light, care, fact,
              //   watering, benchmark, sunlight[], species_id, image, userImage,
              //   addedAt, schedules[] }
              // addedAt: real creation timestamp (set in savePlant), backfilled
              // to Date.now() for pre-existing plants — drives plantMilestoneLabel
              // ("with you for…" callouts) and the plant-anniversary badge
              //   schedules[] }
              // schedules[]: { id, label, everyDays, lastDoneAt, history[] } — user-
              // defined recurring reminders (misting, fertilizing, anything), modeled
              // exactly like watering (lastDoneAt is midnight-anchored like wateredAt,
              // statusOf(days, everyDays) drives its status pill, no unit is stored —
              // the add/edit form's days/weeks toggle collapses to a plain everyDays int)
              // NeedsWaterScreen ("Water" tab) surfaces every plant's due/soon
              // schedules in an "Other reminders" section below the watering
              // list — same due-status math as ScheduleRow (caulis-detail.jsx),
              // reused directly so mark-done there matches the plant-detail
              // behavior exactly; onMarkScheduleDone is markScheduleDone (app.jsx)
locations[]   // known room names (plain strings — see Location tags below for
              // the separate, local-only color/icon overlay keyed by these names)
tab           // 'garden' | 'needs' | 'scanner' | 'print' | 'settings'
detail        // { id, fromScan } | null
form          // { mode:'add' } | { mode:'edit', plant } | null
moveTarget    // plant id | null
menuPlant     // plant | null
queue[]       // plant ids
printed       // bool (transient Print-all confirmation)
```

`days / every` ratio drives status: `>= 1` needs water, `>= 0.7` water soon, else ok.

## Perenual API (`caulis-perenual.jsx`)

- `searchSpecies(q, key)` — `/api/v2/species-list?q=…`
- `getSpeciesDetails(id, key)` — `/api/v2/species/details/{id}`
- `speciesCare(species)` — maps API fields to app shape:
  - `watering` → `every`: Frequent 4 / Average 7 / Minimum 14 / None 21
  - `sunlight[]` → `light` label
  - `default_image.regular_url` → `image`
- No key → uses bundled library (~13 species, same shape as API). Key saved to `localStorage('caulis_perenual_key')`.
- **Resolved**: the CORS/key-exposure warning this section used to carry (production needs a serverless proxy; never expose the key client-side) is fixed — species search/details already went through the `/api/perenual/search` / `/api/perenual/species/:id` backend proxy routes (`backend/src/server.js`), and photo identification joined them in v194 (see below): the client calls its own backend with a garden JWT, the backend looks up the garden's stored key and makes the real upstream call server-side. No client code calls Perenual or PlantNet directly anymore while a garden session exists.

## Photo identify — swallowed errors fixed, real failure surfaced (v193)

- **Root cause of "always says couldn't identify"**: `identifySpecies(dataUrl)` tries PlantNet (`PLANT_ID_KEY`) then falls back to Perenual's `/identify` endpoint (`API_KEY`) — both attempts were wrapped in `try { … } catch(e) {}` with the caught error thrown away, and the function returned a bare `null` on any failure at all: bad/missing key, HTTP error, empty result, network failure, anything. `caulis-detail.jsx`'s `AddPlant` then just set `source = 'failed'` and rendered a static, non-interactive "Couldn't identify" pill with zero detail — nobody, including the user, could tell whether the cause was a missing key, a bad key, a network blip, or a genuine no-match. The Settings → AI copy for the PlantNet key row also actively said **"No key — using demo mode"** even though no demo/offline identify fallback exists anywhere in the code — directly misleading about why identification does nothing without a key.
- **`identifySpecies` now always returns a diagnosable result**: never a bare `null`. On success it returns exactly what it did before (a resolved species record or `{candidates}`); on any failure it returns `{ __failed:true, error }` with a real, specific message — `'No identification key configured — add a PlantNet or Perenual API key in Settings → AI.'` when neither key is set (the actual, most likely root cause for a fresh account — verified: a fresh account has empty `PLANT_ID_KEY`/`API_KEY` by default, nothing seeds one), or the concrete per-service failure otherwise (`PlantNet 401: Bad token`, `Perenual 429: …`, `PlantNet: no plant detected in photo`, etc.), joined with `·` when both services were attempted and both failed. `_plantNetIdentify` now checks `r.ok` and throws with PlantNet's own `{statusCode, message}` body instead of silently treating a 401/429/etc. as "no result."
- **UI**: the "Couldn't identify" pill (`caulis-detail.jsx`, `AddPlant`) is now tappable — toggles a small dark detail bubble below it showing the captured `identifyError`, reusing the exact same `whyToast`/`labToast` dark-pill toast styling already used elsewhere in this codebase (`C.toast` background, `popUp` animation) rather than inventing a new error-surfacing pattern. The pill itself stays calm/small by default; the real message is opt-in via tap, per the user's ask. State resets (`identifyError`/`showIdentifyError` cleared) at the start of every new identify attempt.
- **Settings copy fix**: the PlantNet key row's "no key" subtext (`caulis-screens.jsx`) changed from the misleading "No key — using demo mode" to "No key — photo identification will fail until one is added" — honest about there being no fallback.
- Verified live via a disposable local harness loading the real `caulis-perenual.jsx` (not production, which force-redirects any non-`caulis.czeddaru.dev` origin to the live domain): no-key case returns the actionable "No identification key configured…" message; a deliberately invalid PlantNet key returns the real upstream `PlantNet 401: Bad token` instead of a silent null. Root cause for this specific report is very likely simply **no PlantNet/Perenual key configured on the account** — the fix makes that immediately visible in-app instead of a mystery failure.

## Photo identify — root cause confirmed live, backend proxy shipped (v194)

- **The v193 fix above worked exactly as designed**: it surfaced the real upstream errors, and a live screenshot from the user's phone showed `PlantNet: PlantNet 403: CORS error: Origin not allowed` and `Perenual 405: Method Not Allowed`.
- **PlantNet 403 — confirmed real cause**: this is PlantNet's own server responding with a real HTTP 403 and that literal message in the JSON body (not a browser-blocked opaque CORS failure, which never carries a status/body at all) — PlantNet checks the calling browser's `Origin` header against an allowlist tied to the API key and rejects `caulis.czeddaru.dev`. Exactly the risk the Perenual API section above already flagged. **Fix**: `_plantNetIdentify` (`caulis-perenual.jsx`) now POSTs to a new backend route, `POST /api/identify/plantnet` (`backend/src/server.js`), instead of calling `my-api.plantnet.org` directly, whenever a garden session (JWT) exists — the backend looks up that garden's stored `plant_id_key` (already synced server-side via `/api/garden/keys`, see the custom-color-picker-adjacent note in "Coding Patterns" about `_pushKeyToBackend`) and makes the real multipart PlantNet call from Node, which has no browser `Origin` header for PlantNet to reject. A session-less fallback (direct external call, same as before) remains only for a bare local/dev run with no garden logged in. Verified live via curl straight to `POST https://api.caulis.czeddaru.dev/api/identify/plantnet` with a disposable `claude_test_*` garden and a deliberately fake PlantNet key: got back PlantNet's real `401 Bad token` response (not another Origin-403), proving the call now reaches PlantNet server-to-server and is evaluated purely on key validity, never on Origin.
- **Perenual 405 — confirmed genuinely broken/inaccessible, not a wrong-shape client bug**: direct `curl` testing against `https://perenual.com/api/v2/identify` (POST, various body shapes — JSON, multipart `images[]`, multipart `File[]`) returned `405 Method Not Allowed` with `Allow: GET, HEAD` every time; a GET to the same path returns a generic "Not Found" HTML page, not a JSON API response. Perenual's own docs page for this endpoint (`perenual.com/docs/identify/api`) confirms why: plant identification is a separate **beta program** ("Beta Access Users Only", a "REQUEST BETA ACCESS" button) that a standard Perenual API key is not automatically granted — there is no working, documented request shape for a normal key today. **This is dead, not misconfigured** — the Perenual identify branch was removed from `identifySpecies` entirely rather than left as a fallback that always fails and burns an error line. PlantNet (now reliable via the proxy above) is the sole identify path; `identifySpecies` returns an actionable error naming PlantNet specifically when no `PLANT_ID_KEY` is set, and notes in that same message that Perenual has no working identify endpoint for standard keys, so a user who only ever configured a Perenual key understands why nothing happens.
- **Settings copy**: unchanged from v193's fix (`caulis-screens.jsx`'s PlantNet key row already says "No key — photo identification will fail until one is added") — still accurate, since PlantNet was already the row this app calls "photo identification."
- Not fully end-to-end verified with a real successful species match (no live PlantNet/Perenual API key was available in this environment) — what **was** verified live: the app loads and runs with zero JS errors after these changes (Playwright against a local static-file harness); the new backend route is deployed and live on the VM (`systemctl status caulis-backend` healthy, `/health` responds); a direct curl against the deployed proxy route with a real disposable garden + a syntactically-valid-but-fake PlantNet key returns PlantNet's genuine `401 Bad token`, confirming the origin-rejection is gone and the server-to-server call shape (multipart form, `api-key` query param, `lang`) is correct against PlantNet's real API. A user with a valid PlantNet key should now get real identification results; if any exotic gap remains it would only surface with a real key in hand.

## Service Worker

`sw.js` line 1: `const CACHE = 'caulis-vN'` — bump N every time any file changes. Current: v194.
Keep `APP_VERSION` in `caulis-core.jsx` in sync with the `sw.js` CACHE number.

Push payloads carry a `type` (`watering` | `reminder` | `digest`) that the `push` handler uses to pick a per-type `vibrate` pattern and a stable `tag` (with `renotify:true`, so a second same-type push replaces rather than stacks). Single-item watering/reminder pushes carry per-action signed tokens in `data` (`actionToken` for water/schedule-done, `snoozeToken` for snooze) minted by `signActionToken(gardenId, plantId, action, extra)` (`backend/src/auth.js`) — `notificationclick` looks up the right token for `e.action` and POSTs it + the action name to `/api/push/action`, which validates the signed action matches the button pressed before mutating anything. iOS Safari PWAs ignore `vibrate` entirely (no web-push vibration API) and don't render `image`/big-picture banners — richness there is limited to title/body/icon/actions; Android/Chrome gets the full set.

`push_subscriptions.watering_frequency_days` (default 1 = daily) lets a garden pick the watering/custom-reminder push cadence independent of its time-of-day — Settings → Notifications → "Watering ping frequency" is a real `<input type="range">` (1-14 days, not a segmented control). `checkAndSendPushes` in `backend/src/server.js` gates the send on both "not already sent today" AND elapsed days since the last send `>= watering_frequency_days`. Digest keeps its fixed weekly cadence (day-of-week picker only, no separate frequency column).

## Push notification bugs found and fixed (v191, same day as the frequency slider above)

**Root cause of "notifications don't work" (a repeat report, previously un-found despite log/DB checks)**: `last_watering_sent_on`/`last_digest_sent_on` are Postgres `DATE` columns. node-pg returns a `DATE` column as a real JS `Date` object, not the `"YYYY-MM-DD"` string `midnightFromStamp()` expects (that shape only ever came from `plant.history[]` entries, which live in JSONB, not a DATE column). `String(dateObject).split('-')` silently produced `NaN`, and once a garden's first-ever watering push succeeded (setting the column to a real value), every cadence check on every subsequent tick compared against `NaN` and evaluated false forever — permanently jamming that garden's watering pushes after day one. Confirmed live on the real VM: `push_subscriptions` id 3 (a real garden with 7 plants overdue) had been stuck at `last_watering_sent_on = 2026-07-28` for two full days despite the 15-minute cron running the whole time; a direct `webpush.sendNotification()` test to that same subscription succeeded immediately (VAPID keys, subscription validity, and FCM delivery were all fine — this was never a "can't reach the device" problem). A **second, independent bug** in the same function: the digest gate used `sub.last_digest_sent_on !== todayStr`, comparing a `Date` to a string — always `!==`, so on an actual digest day the "already sent today" check never actually held, meaning a garden could get the weekly digest re-sent on every single 15-minute tick all day (the opposite failure mode — the exact spam pattern this system is built to avoid). Both are root-caused to the same type mismatch, both fixed by a shared `pgDateMs(val)` helper that handles a real `Date` or a plain string. `sendPush` (`backend/src/server.js`) also used to swallow every non-404/410 failure with zero logging at all — the actual reason prior investigations "checked out" from logs but the bug persisted; it now logs `{subId, gardenId, statusCode, body}` via `app.log.warn` on any real send failure.

**Multi-per-day pushes** (`push_times_per_day`, 1-3, default 1, Settings → Notifications → "Pushes per day" segmented 1×/2×/3×): a genuinely new axis from `watering_frequency_days` (which only ever goes from daily to less-frequent) — lets a garden opt into a morning + evening (or +midday) nudge. Deliberately capped at 3 and opt-in-only per this app's standing anti-spam rule. When active it bypasses the day-based `reminder_hour_utc`/`watering_frequency_days` gate entirely and instead fires whenever `(24 / push_times_per_day)` hours have elapsed since `last_watering_sent_at` (a new `TIMESTAMPTZ` column, distinct from the `DATE` columns above) — the Settings UI visibly dims/disables the frequency-days slider whenever pushes-per-day is above 1 so the two controls never fight over the same subscription. `pushTimesPerDay` threads through `caulis-firebase.jsx`'s `pushSubscribe`/`pushSetPrefs` and `app.jsx`'s `ensurePushSubscription`/`syncPushPrefs` exactly like `wateringFrequencyDays` already did.

## In-app notice toggles (v191)

Distinct from the push-notification system above — these gate purely client-side toast/banner UI, never anything server-sent. Settings → Notifications → "In-app notices": **"New version available"** (`noticeUpdateReady`, `caulis_notice_update`, default on) gates only `updateReadyEl`'s "Reload" banner — the underlying stale-shell detector/service-worker update logic is untouched, so a suppressed banner doesn't stop the app from actually updating on its own next reload. **"Sync status notices"** (`noticeSyncStatus`, `caulis_notice_sync`, default on) gates both `syncErrorEl` ("saved on this device only until back online") and `syncMergedEl` ("changed elsewhere, merged automatically") as one pair — both are pure connectivity/merge-state information with zero data loss, so both are freely toggleable. **`storageFullEl` (photos may not have actually saved) deliberately has no toggle** — it is the one notice in this set that flags a real, silent data-loss risk, so it always shows regardless of these settings; the "In-app notices" section says so explicitly.

## Notification settings & custom reminders

`push_subscriptions` (backend) carries per-garden schedule: `reminder_hour_utc` (default 8, converted client-side from the local hour shown in Settings → Notifications via `localHourToUtc`/`utcHourToLocal` in `app.jsx`), `digest_day_of_week` (0=Sun..6=Sat, default 1/Monday), `custom_reminders_enabled` (separate toggle from watering/digest — folds due custom reminders into the single daily push instead of sending a second one). `checkAndSendPushes` (`backend/src/server.js`) gates per-row against these instead of a hardcoded global time. `isDue(anchorMs, everyDays, snoozedUntil)` is the one shared due-calculation used by both watering (`plantNeedsWater`) and custom schedules (`dueSchedules`) — never duplicate the days-since-midnight math again. `buildWateringPush`/`buildDigestPush` combine watering + due schedules into one push per garden per day (never one push per reminder kind — that recreates the original spam pattern) and name a couple of plants/schedules when short (`listNames`), falling back to a count only when long.

## Behavior settings

Settings → Behavior: `confirmDelete`, `swipeNav`, `haptics` (all simple booleans — kept as single toggles on purpose, no per-action granularity added; the existing global toggle already covers the one real use case cleanly). **Haptic strength** (`hapticIntensity`, `caulis_haptic_intensity`, `HAPTIC_INTENSITIES`/`HAPTIC_INTENSITY_ORDER` in `caulis-core.jsx`, sub-control revealed only while Haptics is on): `gentle`/`standard`/`firm`, a multiplier applied to `fireHaptic(kind, mult)`'s (`app.jsx`) existing `navigator.vibrate` ms patterns — the iOS Taptic-switch fallback path has no duration to scale, so intensity only reaches Android/desktop. Silent/tactile only, deliberately separate from sound (never added — same call as an earlier pass).

**Instant-preview on change (v193)**: `setHapticIntensity` (`app.jsx`) now fires a real `fireHaptic('medium', mult)` sample pulse at the moment a new strength is picked, gated on `haptics` being on — vibration has no visual readout, so a "pick it and feel it right now" trigger is the only way to compare gentle/standard/firm without leaving Settings and doing a real watering action elsewhere. Verified the three multipliers (0.55/1/1.7) produce distinct scaled `navigator.vibrate` ms values. Other candidate settings audited for the same "instant preview" treatment (icon stroke, grain/texture intensity, status-indicator style, nav bar indicator style) were all found to already have organic live feedback for free — they mutate the same module-level `C`/token state (`applyIconStroke`/`applyGrainIntensity`/`applyStatusStyle`/etc.) that Settings itself re-reads every render, and the always-mounted `BottomNav`/grain overlay/status pills are visible immediately as the value changes — so no artificial "test" trigger was added there, only to haptics, which is the one genuinely non-visual setting in this group.

Swipe-to-water/snooze thresholds (`WATER_THRESH`/`SNOOZE_THRESH` in `NeedsRow`, `caulis-screens.jsx`) were deliberately left untouched — that gesture code has caused real production bugs before and a sensitivity preference wasn't worth the risk given the time available. Garden sort (`caulis_g_sort`) already persists across sessions via `GS.set`/`GS.get` — no work needed there, it doesn't reset.

**Card date line** (`cardDateMode`, `caulis_card_date_mode`, `CARD_DATE_MODES`/`CARD_DATE_MODE_ORDER`): Settings → Garden. `last` (default) keeps `PlantCard`'s small date line as `agoLabel()` ("Watered 3 days ago"); `due` swaps it to `dueLabel(days, every, compact)` (`caulis-core.jsx`) — a pure countdown ("Due in 4 days" / "Overdue 2d") derived from the same `days`/`every` ratio `statusOf` already uses, no new field on the watering model.

## Watering Model

`wateredAt` (absolute midnight epoch) is the source of truth; `days` is derived via `daysSinceMidnight()`. `deriveWateredAt(p)` in `caulis-core.jsx` trusts a stored `wateredAt` only when `p.wv === WATER_SCHEMA`, else recomputes from the last `history` stamp, else `today - days`. Bump `WATER_SCHEMA` to invalidate previously-stamped data after a model change. Never re-introduce a synthetic day offset in the derive — it corrupts waterings synced from un-updated clients.

## Developer Panel

Hidden in Settings → About: tap the Version row 7× to reveal a "Developer" section, gated by a PIN (`localStorage caulis_dev_pin`, set on first use). Tools: water-all, bulk day shift, per-plant day set for the current garden, plus cross-garden node management (load any garden by key+password, edit dates in memory, push back). Client-side obscurity only — not real auth. "Lock & hide" clears `caulis_dev_revealed`.

## Doctor (AI plant chat)

`DoctorOverlay` (`caulis-detail.jsx`) — vision chat over the Anthropic API via `doctorAsk()` (`caulis-perenual.jsx`, raw `fetch` + `anthropic-dangerous-direct-browser-access`). Reuses the existing `anthropicKey`. Carries the same `Sprig` botanical watermark every other full-screen overlay has (was missing it — a same-day audit found and fixed the gap). Model chosen in Settings → AI (`caulis_doctor_model`, default `claude-haiku-4-5`, toggle Haiku/Sonnet). Capped thread: last 3 exchanges (6 msgs) resent each call; photo attached as a base64 image block on its user turn. Entry points: nav action `doctor` (standalone, photo-only) and a "Ask the doctor" button on `PlantDetail` (passes plant data as `plantContext`). Both nav actions `add`/`doctor` route through `onNavAction` in `app.jsx`.

**Agentic tools** (`DOCTOR_TOOLS` in `caulis-perenual.jsx`): `list_garden_plants` (lazy garden read — only sent when needed, keeps tokens low) and `suggest_correction` (proposes field edits to a saved plant). `doctorAsk({withTools})` returns `{content, stop_reason, text, toolUses}`; the overlay runs a capped 4-hop tool loop (`runTool`), executing tools client-side and feeding `tool_result` back. Tool plumbing is NOT persisted to `thread` — only final text + correction cards — so future turns don't resend it. A `suggest_correction` renders a polished accept/dismiss card; Apply calls `onApplyCorrection(plantId, changes)` → `applyCorrection` in `app.jsx` (mirrors edit-path `every`→`benchmark`).

**History**: last 3 chats persist to `localStorage('caulis_doctor_chats')` via `saveDoctorChats` (strips images oldest-first on quota). Overlay resumes the most recent chat for the current plant (or most recent overall) on open; header has new-chat (+) and history (clock) buttons. **Markdown**: assistant replies render through `MarkdownText` (bold/italic/code/bullets/numbered/headings); the latest reply types out via `TypewriterMarkdown` (~2.4s reveal, balances dangling `**`, blink caret). Premium polish: `doctorIn` fade-rise on bubbles, `bar` equalizer "examining…" indicator (keyframes in `index.html`). System prompt tightened to lead with the answer, ~120-word cap.

## Bottom Navigation

Customizable via Settings → Navigation bar. `navConfig` (`caulis_navbar`) is an array of `{action, center?}` slots normalized by `normalizeNav()` (`caulis-core.jsx`), up to `NAV_MAX` (7); editor supports add/remove/reorder/center. Actions in `NAV_ACTIONS`/`NAV_ORDER`. Tab actions call `setTab`; non-tab actions (`add`, `doctor`, `more`) call `onNavAction`. **Swipes and launch-tab respect the bar**: `navTabOrder(navConfig)` drives both the swipe `TAB_ORDER` and a mount effect that snaps to the first bar tab if the saved tab isn't present. `more` opens a sheet (`moreEl` in `app.jsx`) listing every action — guarantees access to anything not on the bar. Desktop uses `DesktopSidebar` (nav slots/order/labels shared with mobile; its own layout — position/collapse/width/footer — is customizable separately, see below).

Nav icons already ran every stroke literal through `isw()` (icon-stroke-weight) before this pass — no separate wiring needed, it was already consistent.

**Active-tab indicator** (`navIndicatorStyle`, `caulis_nav_indicator`, `NAV_INDICATOR_STYLES`/`NAV_INDICATOR_ORDER` in `caulis-core.jsx`): `tint` (default — just colors the active icon/label, the original look), `underline` (a short accent bar under the active icon), `pill` (a soft filled pill behind it). Applies to `BottomNav`'s regular (non-center) slots and to `DesktopSidebar`'s `SidebarItem` rows (mapped there to: tint's own left-accent bar / pill background / underline border-bottom). The raised center slot keeps its own dedicated raised-circle treatment regardless of this setting — that's a separate, already-established visual device. Independent axis from icon-stroke-weight (line boldness) and radius density (corner rounding scale).

**Nav bar shape** (`navBarStyle`, `caulis_nav_shape`, `NAV_BAR_STYLES`/`NAV_BAR_STYLE_ORDER`): mobile-only choice between `flat` (default, edge-to-edge bar anchored to the bottom safe area) and `floating` (rounded, inset from the screen edges with its own shadow, reading as an island). A container-silhouette axis, distinct from radius density which scales corner rounding proportionally everywhere rather than repositioning/reshaping one specific surface.

## Doctor — garden photos

Opening the doctor from a plant pre-seeds that plant's saved photo (`firstPhoto`) so you can ask without snapping a new one; `activePlant` (state, init from the `plant` prop) drives context + `suggest_correction` target and updates a chip in the composer. A leaf button in the composer opens a garden picker sheet (`pickGardenPlant`) to attach any saved plant's photo + set it as context. `imgBlock()` builds the Anthropic image block from a dataURL (base64) or a remote https URL, so both stored user photos and API images work.

## Animations

- Overlay slide-in: `translateY(26px → 0)` + fade, 320ms `cubic-bezier(.2,.8,.2,1)`.
- Card press: `scale(0.975)`, shadow softens, 180ms.
- Long-press threshold: 480ms; suppresses the subsequent click.
- Scan line: 2.4s `ease-in-out` loop. Spinner: 0.9s `linear` rotate.
- Undo pill: `popUp` spring animation.
- Print-all "Sent" state: 2600ms timeout.
- Card entrance: `cardIn`, `translateY(10px)→0` + fade, 240ms `MOTION.out`, 28ms stagger, capped at 10 cards (`cardEntranceStyle`).
- Shimmer sweep (`shimmerStyle`, `caulis-core.jsx`): `linear-gradient` sweep, 1.4s linear infinite — for async image-loading slots.
- Hero crossfade: `heroIn`, 420ms `cubic-bezier(.2,.8,.2,1)` on initial mount; a live rotation instead cross-dissolves outgoing/incoming picks over 420ms (`opacity`/`fade`, see Garden hero banner above). Ken Burns: `kenBurns`, one-way `scale(1)→scale(1.06)`, 16s linear — Garden hero banner's featured tile + Plant Detail hero frame only, never the actively-swiped carousel. Hero banner's smaller supporting collage tiles use the gentler `kenBurnsTile`, `scale(1)→scale(1.03)`, 16s linear.
- Watering micro-celebration: `waterPulse` (240-260ms spring) + `waterGlow` (700ms ease-out) + `driftUp` particles (600-700ms ease-out) — smaller than and distinct from the water-all/badge confetti.
- Toggle confirm: `toggleFlash`, 500ms ease-out ring, fires on every `ToggleKnob` value change.
- Empty-state bob: `sproutBob`, 4.5-5s ease-in-out infinite.
- Every animation above is `reduceMotion`-gated, either via the app's own JS check or the blanket `html[data-rm="1"] *` CSS override (`index.html`) that forces `animation-duration: 0.001ms` / `iteration-count: 1` — both exist, don't remove either.

## Depth pass 3-5: organic grain, primary-CTA press states, moon phase, rare found-object (v196)

- **Grain "repeating" complaint — root cause**: `#grainFilter` (`index.html`) drove `.grain-overlay` from a single `feTurbulence` at one `baseFrequency`, stretched full-viewport — a known `feTurbulence` artifact where one frequency/octave combo reads as a faint quasi-regular cell pattern once scaled up, which is what registered as "repeating." **Fix**: a second, independent `feTurbulence` (`#grainTurb2`, unrelated frequency — roughly `freq * 0.44`, deliberately not a clean fraction, own `seed`) composited with the first via `feBlend mode="overlay"` before the existing alpha-only `feColorMatrix`. Still pure SVG filter, no image asset, still theme-neutral. `applyGrainIntensity()` (`caulis-core.jsx`) now mutates both `#grainTurb` and `#grainTurb2` — marble mode scales both the same way it already scaled the first. Verified live (disposable local harness, redirect bypassed): both nodes carry the expected frequencies at runtime; a high-contrast render of the blended filter (colorMatrix alpha boosted, normal blend mode over a white body) shows fine, non-periodic speckle with no visible cell/grid structure.
- **Primary-CTA press states**: `usePressScale(reduceMotion)` (`caulis-core.jsx`) — shared hook, same motion budget as the existing card press (`scale(0.975)`, 180ms `cubic-bezier(.2,.8,.2,1)`, box-shadow softens). Wired to exactly two buttons, per the standing anti-scope-creep rule — PlantDetail's "Mark as watered" CTA and `AddPlant`'s save bar CTA (`caulis-detail.jsx`) — not every tappable row. `reduceMotion` threaded into `AddPlant` (previously didn't receive it; now passed from `app.jsx`). Verified live via real `pointerdown`/`pointerup` dispatch + `getComputedStyle` read mid-transition on both buttons: `transform` animates toward `scale(0.975)` and `box-shadow` softens to the pressed value on press, reverts on release.
- **Moon phase glyph**: `moonPhaseFraction(date)`/`moonPhaseLabel(frac)`/`MoonPhaseGlyph` (`caulis-core.jsx`) — plain synodic-month math (known new-moon reference + 29.53059-day period), zero network/API call. Rendered small and quiet next to `GardenScreen`'s greeting line (`todayGreeting()`), themed via `C.brown`, native `title` attribute carries the phase name on hover — pure decoration, no state, no logic elsewhere reads it. Verified live: renders next to the greeting, `title="Waning gibbous"` (correct for the test date) confirmed via accessibility snapshot.
- **Rare found-object detail**: `Sprig` (`caulis-core.jsx`) gained a ~3% chance per mount of drawing a tiny ladybug resting on its base leaf — decided via a lazy `useState(() => Math.random() < 0.03)` initializer, never persisted/tracked/counted anywhere (no localStorage key, no "collection," nothing gamified). `pointerEvents:'none'` on the ladybug group keeps it out of the hit-test even on the one `Sprig` instance that's tappable (`onTap`, Garden screen's 7-tap-style trigger) — purely cosmetic, zero interaction, zero effect on any logic.
- Bumped `APP_VERSION` (`caulis-core.jsx`) and `sw.js` `CACHE` to 196 together, per the standing rule.

## Custom texture controls + per-element burnt edges + organic vein rework (v197)

- **Custom texture controls** (`caulis_texture_custom`, off by default, Settings → Appearance → Background & motion): reveals a slider panel below the existing preset pickers, same conditional-reveal pattern as the custom color picker/grain-intensity sub-controls. Four `<input type="range">` sliders (`TextureSlider`, `caulis-screens.jsx`, styled to match the existing "Watering ping frequency" slider exactly) — **Grain amount**, **Grain coarseness**, **Vignette strength**, **Burnt edges** — plus a **Pattern density** slider that only shows when `bgTexture` is `dot`/`vein`. Storage: `caulis_texture_grain_amt`/`_grain_coarse`/`_vignette`/`_burnt`/`_pattern_scale`, all 0-100. `applyCustomTexture(enabled, grainAmt, grainCoarse, vignetteAmt, burntAmt, patternScaleAmt)` (`caulis-core.jsx`) called every render from `app.jsx`, same apply/read pattern as every other `caulis_*` customization. While `enabled` is false it never touches the DOM — zero regression, the preset pickers stay the only thing in effect.
- **Grain amount/coarseness/vignette strength** override the existing global `--grain-opacity`/`#grainTurb`/`#grainTurb2`/`--vignette-opacity` continuously instead of snapping to a `GRAIN_LEVELS` preset step — same DOM nodes, same imperative `baseFrequency` mutation reasoning as `applyGrainIntensity()`, just fed continuous slider math instead of a preset lookup. Deliberately decoupled from the `bgTexture` paper/marble gate — advanced mode is meant to fully replace preset-picking, not require "Paper grain" to also be selected.
- **Burnt edges — shipped, reverted, and rebuilt once live-tested (three iterations this session)**:
  1. First attempt: a fixed full-viewport `.burnt-overlay` div (four corner `radial-gradient`s + an SVG `feDisplacementMap`/`feTurbulence` filter, `#burntFilter`), mirroring the grain/vignette overlay pattern. A live screenshot at 100 showed a huge soft fog/cloud instead of scorched corners — two real bugs: `radial-gradient(circle at 0% 0%, …)` with no explicit size defaults to the `farthest-corner` sizing keyword, so every percentage stop was actually relative to the full viewport diagonal, not a small corner radius; and the turbulence `baseFrequency`/displacement `scale` were tuned for smooth cloud-scale features, not edge-scale raggedness. First fix pass (explicit `circle 34vmin` sizing, higher frequency, smaller displacement) made it look like genuine scorched corners at screen-edge scale — but user feedback then called the whole *architecture* wrong regardless of tuning.
  2. **Root correction**: a single page-wide scorch reads as an odd wash over the entire app chrome (nav bar, search bar, everything), not a considered photo treatment — burnt paper corners only make sense on an actual photo-like surface. The global `.burnt-overlay`/`#burntFilter`/`--burnt-opacity` machinery was removed entirely from `index.html`; `applyCustomTexture()` no longer writes anything burnt-related to the DOM.
  3. **Final architecture**: `burntEdgeStyle(intensityMult)` / `grainTextureStyle(intensityMult)` (`caulis-core.jsx`) — plain style-object helpers following the *exact* `warmEdgeStyle(strength)`/`PHOTO_FRAME_SHADOW` pattern already established for photo frames: each call bakes the live `customBurntAmt`/`customGrainAmt` (read directly off the module-level `let`s `applyCustomTexture` already maintains, no new prop threading) into a **self-contained** SVG data-URI — its own embedded `<filter>`/`feTurbulence`/`feDisplacementMap`, no shared DOM node — using the same inline-data-URI technique as `dotPatternUri`/`veinPatternUri`. `burntEdgeUri` draws one corner gradient (dark brown-black, `rgba(20,10,4,…)`, staying in the app's existing warm vocabulary) mirrored into all four corners via `transform="scale(-1,1)"` etc., run through a small-scale `feDisplacementMap` for organic raggedness at *element* scale rather than viewport scale. `grainTextureUri` is a small tileable turbulence rect, applied `mixBlendMode:'overlay'`.
  - **Where it's applied** — call sites that already carry the `warmEdgeStyle`/`PHOTO_FRAME_SHADOW` photo-frame treatment, each adding one more absolutely-positioned overlay div: `Specimen` (`caulis-core.jsx`, the universal plant-photo tile — reaches Garden cards, Print Queue, the Digest, everywhere with zero per-screen wiring, at a toned-down `0.55x` multiplier since a ~150px thumbnail reads "scorched" at much lower intensity than a full-width banner), `GardenHeroBanner`'s collage (`caulis-screens.jsx`, `1x`), `DigestHeroCollage` (`caulis-screens.jsx`, `1x`), and Plant Detail's `PhotoCarousel` hero frame (`caulis-detail.jsx`, `0.8x`). At `caulis_texture_burnt`/`caulis_texture_grain_amt` = 0 (the default) every one of these renders `backgroundImage:'none'` — fully inert until a user opts in.
  - Verified live (disposable local harness, redirect bypassed, seed plants given a temporary `userImage` since the bundled seed data ships with no photos): at burnt=100/grain=80 every plant card and the hero banner show visibly ragged dark corners with fine grain across the photo area, name/location/date text stays fully legible below, and — critically — the rest of the screen (nav, search bar, filter chips) is completely untouched, which is the whole point of the per-element correction.
- **Pattern density** (`caulis_texture_pattern_scale`): a `patternScaleMult` module-level multiplier (1 when custom mode is off) read by `bgTextureStyle()`'s `backgroundSize` for `dot`/`vein` only — 0.65x-1.55x of the base tile size, capped so "dense" never shrinks into a moiré mess and "sparse" never spreads so far it looks empty.
- **`veinPatternUri` rebuilt for organic continuity, not just "more variety"** (`caulis-core.jsx`): even the v195 fix (420px tile, 5 varied motifs) still read as discrete floating leaf clusters rather than one continuous vine surface once checked live at real viewport sizes. Rebuilt around two long, irregular multi-segment-bezier vine strokes that run edge-to-edge across a widened 520px tile (`VEIN_TILE`), with the leaf motifs now sprouting FROM those vines via short connector stems instead of floating independently. **Seamless tiling handled generally**: rather than hand-placing motifs away from edges, the entire tile's content (vines + motifs) is rendered 9× at every combination of `-VEIN_TILE/0/+VEIN_TILE` translation, then clipped to the single-tile viewBox — any shape crossing an edge reappears via its neighboring-tile copy at the exact matching position, so vines visibly continue into the next tile with zero seam. This is a general technique (works for any content, not just hand-tuned edge-safe shapes) and could be reused for future pattern work. Verified live at both a 390px mobile viewport and a 1600×1200 desktop viewport with the vein texture + white background active: the vine network reads as one continuous, irregular, organic growth across the whole visible area with no visible tile repeat or seam.
- Bumped `APP_VERSION` (`caulis-core.jsx`) and `sw.js` `CACHE` to 197 together.
